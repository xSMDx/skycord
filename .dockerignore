# Everything the image does not need, and everything it must never contain.
node_modules
dist
.git
.github
.claude
.playwright-cli
.playwright-mcp
docs
landing
coverage
*.log

# Secrets: these live on the host and arrive as environment variables or mounts.
.env
.env.*
!.env.example
voice-servers.json
